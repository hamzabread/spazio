{
  "name": "spazio-submit-server",
  "version": "1.0.0",
  "description": "Backend endpoint that receives press-release submissions from the frontend form and creates a draft post in Ghost via the Admin API.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@tryghost/admin-api": "^1.13.12",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
