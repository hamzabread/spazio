{{!< default}}

{{!--
  Select this template in Ghost Admin → Page settings → Template → "Jobs"
  if the page slug is not "jobs" (otherwise page-jobs.hbs is used automatically).
--}}
{{#post}}
{{> "jobs-page-body"}}
{{/post}}
